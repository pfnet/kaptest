version: v1alpha1
policies:
- ../vap-with-params.yaml
resources:
- resources.yaml
vapTestSuites:
- policy: deployment-replicas
  tests:
  - object:
      kind: Deployment
      name: ok
    param:
      name: config1
    expect: admit
  - object:
      kind: Deployment
      name: bad
    param:
      name: config1
    expect: deny
    deniedMessage: "replicas must be equal or less than 5"
