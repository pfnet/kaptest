---
# Empty document before the CRDs.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: shelves.example.com
# listKind is intentionally omitted.
spec:
  group: example.com
  names:
    kind: Shelf
    # Use an irregular plural to verify resource names come from this field, not kind + "s".
    plural: shelves
  scope: Namespaced
  versions:
  - name: v1
    served: true
    storage: true
    schema: &shelfSchema
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              parts:
                type: array
                x-kubernetes-list-type: map
                x-kubernetes-list-map-keys: [name]
                items:
                  type: object
                  required: [name]
                  properties:
                    name:
                      type: string
                    value:
                      type: string
              tags:
                type: array
                x-kubernetes-list-type: atomic
                items:
                  type: string
  - name: v2
    served: false
    storage: false
    schema: *shelfSchema
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: shelves.other.example.com
# listKind is intentionally omitted.
spec:
  group: other.example.com
  names:
    kind: Shelf
    # Use an irregular plural to verify resource names come from this field, not kind + "s".
    plural: shelves
  scope: Namespaced
  versions:
  - name: v1
    served: true
    storage: true
    schema: &shelfSchema
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              parts:
                type: array
                x-kubernetes-list-type: map
                x-kubernetes-list-map-keys: [name]
                items:
                  type: object
                  required: [name]
                  properties:
                    name:
                      type: string
                    value:
                      type: string
              tags:
                type: array
                x-kubernetes-list-type: atomic
                items:
                  type: string
  - name: v2
    served: false
    storage: false
    schema: *shelfSchema
